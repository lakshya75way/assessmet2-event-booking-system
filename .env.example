# API Configuration
# Replace with your actual backend URL. Defaults to "/api" which works with the built-in mocks.
VITE_API_BASE_URL=/api

# Mock Service Worker (MSW) Configuration
# Set to "false" to disable mocks and connect directly to the VITE_API_BASE_URL
VITE_ENABLE_MOCKS=true
